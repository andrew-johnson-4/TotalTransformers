import $"preludes/l1.tlc";

let expr: String = match argv {
   ("eval", expr) => expr,
   _ => {
      print("help");
      print("\teval [expr]");
      exit(0); ""
   },
};

match expr {
   f"{x}+{y}" => {
      print("TODO: plus");
   },
   t => {
      eprint(f"Error, invalid expression: {t}");
      exit(1);
   },
};
