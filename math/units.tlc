import $"preludes/l1.tlc";

let expr: String = match argv {
   ("eval", expr) => eval(expr),
   _ => {
      print("help");
      print("\teval [expr]");
      exit(0); ""
   },
};

let eval(s: String) {
   match expr {
      f"{x}+{y}" => {
         print("TODO: plus");
         exit(0);
      },
      t => {
         eprint(f"Error, invalid expression: {t}");
         exit(1);
      },
   }
};
